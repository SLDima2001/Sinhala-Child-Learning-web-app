import React from 'react';

const Presentations = () => {
  return (
    <div className="section animate-fade-in" style={{ paddingTop: '8rem' }}>
      <div className="container">
        <h2 className="text-center text-gradient" style={{ fontSize: '3rem', marginBottom: '3rem' }}>Presentations</h2>
        <div className="glass-panel" style={{ padding: '2rem' }}>
          <p style={{ color: 'var(--text-secondary)' }}>Slides from past and future presentations.</p>
        </div>
      </div>
    </div>
  );
};

export default Presentations;
